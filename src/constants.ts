export const BOARD_VIEW_TYPE = "pm-board";

export const CSS_PREFIX = "pmb";

/** Frontmatter property holding the manual drag order of a card. */
export const DEFAULT_ORDER_PROPERTY = "kanban_order";

/** Label shown for the group of entries whose groupBy value is empty. */
export const NO_VALUE_LABEL = "(No value)";
